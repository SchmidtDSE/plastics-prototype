/**
 * Template for the ANTLR visitors.
 * 
 * @license BSD, see LICENSE.md
 */

import {CONSUMPTION_ATTRS, EOL_ATTRS} from "const";

// eslint-disable-next-line no-undef
let toolkit = null;

try {
    toolkit = PlasticsLang.getToolkit();
} catch (e) {
    alert('Sorry, we could could not initialize app. Your browser may be out of date.');
    console.log(e);
}


{{ CODE }}


// eslint-disable-next-line no-undef
export {toolkit, CompileVisitor};
